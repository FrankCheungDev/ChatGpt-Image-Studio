# Docker Compose startup settings.
# Copy this file to .env, then edit the values before production deployment.

HOST_PORT=7000
IMAGE_TAG=local
IMAGE_COMMIT=local
IMAGE_BUILD_TIME=local
TZ=Asia/Shanghai

# Used only when the user database is empty on first startup.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password
